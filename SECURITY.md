# Security

Open AutoClicker is a local-only Windows utility. It does not perform network requests, telemetry, analytics, update checks, startup registration, persistence, credential access, or background data collection.

Before publishing a modified version, review changes for added networking, hidden persistence, personal-data handling, or machine-specific paths.
